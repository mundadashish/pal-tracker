---
applications:
  - name: pal-tracker
    path: build/libs/pal-tracker.jar
    random-route: pal-tracker-${UNIQUE_IDENTIFIER}.${DOMAIN}
    env:
      WELCOME_MESSAGE: Hello from Cloud Foundry
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'
